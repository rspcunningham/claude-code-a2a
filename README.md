# Claude Code SDK Test with A2A and Docker

Required env vars:
 - ANTHROPIC_API_KEY

To launch the server:

```bash
docker-compose build
docker-compose up
```

To launch the client:
```bash
uv run a2a_client.py
```
