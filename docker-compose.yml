services:
    heartbeat-logger:
        build: .
        command:
            - uv
            - run
            - python
            - heartbeat_receiver.py
        environment:
            HEARTBEAT_PORT: 8080
        ports:
            # expose heartbeat logger for optional host inspection
            - target: 8080
              published: 0
              protocol: tcp
        restart: unless-stopped
